# El método .sort() devuelve la lista en orden
numbers = [1,3,5,2,4]

numbers.sort()
print(numbers)

# *Si quisieramos ordenar los elementos en orden decreciente podríamos usar "reverse" y "sort"
numbers.sort(reverse = True)
print(numbers)